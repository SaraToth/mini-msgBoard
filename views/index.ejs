<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% for(let i = 0; i < messages.length; i++) { %>
            <li>
                <%= messages[i].user %> (<%= messages[i].added %>)<br>
                <%= messages[i].text %>
            </li>
        <%}%>
    </ul>
</body>
</html>