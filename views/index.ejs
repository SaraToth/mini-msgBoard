<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/indexStyles.css">
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% for(let i = 0; i < messages.length; i++) { %>
            <li>
                <div class="wrapper">
                    <span class="user"><%= messages[i].user %></span> <span class="date">(<%= messages[i].added %>)</span><br>
                    <div class="message"><%= messages[i].text %></div>
                </div>
                <a href="/message/<%= messages[i].id %>">Open</a>
            </li>
        <%}%>
            <li>
                <a href="/new" class="btn">Add New Message</a>
            </li>
    </ul>
</body>
</html>